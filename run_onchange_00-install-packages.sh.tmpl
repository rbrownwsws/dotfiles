{{ if eq .chezmoi.osRelease.idLike "debian" -}}
#!/usr/bin/env sh
echo "### Install packages ###"
sudo apt update
sudo apt install -y \
    fzf \
    zsh \
    ripgrep \
    fd-find \
    jq
{{ end -}}
